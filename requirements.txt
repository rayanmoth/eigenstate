# Vercel installs this at build time. Measured unzipped footprint of this
# set is around 250 MB against a 500 MB Python limit, so there is headroom
# but not a lot -- do not add anything casually.
flask
qiskit
qiskit-aer
requests

# NOT listed on purpose: qiskit-ibm-runtime and qiskit-experiments.
# MothQuantumGraph is API-identical to QuantumGraph and needs neither, and
# it is first on sys.path so it wins. If the build fails on an import from
# inside quantumgraph, add them here -- but check the size first, because
# qiskit-ibm-runtime is not small.
