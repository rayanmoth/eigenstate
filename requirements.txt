# Vercel installs this at build time.
flask
qiskit
qiskit-aer
requests

# NOT optional, despite what QuantumGraph's own requirements.txt says.
# QuantumGraph imports qiskit_ibm_runtime at MODULE level while omitting it
# from its dependencies, so importing quantumgraph fails without it. This
# was invisible locally because quantumfaction-env has it installed;
# deployment is the first environment that did not.
#
# Nothing in Eigenstate uses IBM's runtime -- the backend is quantumgraph's
# own ExpectationValue (exact, local) or Moth's HTTP API. This is here
# purely to satisfy an import.
qiskit-ibm-runtime

# Same story if the traceback moves on to this one. Left commented because
# the bundle was already 249.85 MB and every package counts; uncomment only
# if the log names it.
# qiskit-experiments
